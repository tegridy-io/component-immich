# Overwrite parameters here

parameters:
  immich:
    components:
      server:
        ingress:
          url: fotos.tegridy.io
      valkey:
        enabled: true
      machineLearning:
        enabled: true
